#include <stdio.h>
#include<string.h>
int main()
{
	char str[80];
	float length=120;
	printf("Enter the string:\n");
	gets(str);
	length = (length - strlen(str))/ 2;
	for (int i = 0; i <=length ; i++)
	{
		printf(" ");
	}
	printf("%s", str);
	return 0;
}
